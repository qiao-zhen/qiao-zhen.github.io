# 飞书推送 · 阿里云函数（FC）部署指南

目标：让你在手机上点一下桥桥工作台的「导出到飞书」，笔记就直接写进你的 4 个飞书文档，**不用电脑、不用把数据发给我**。

整套逻辑：网页只负责把 4 类笔记 POST 给一个服务端函数；函数用**存在阿里云上的密钥**调飞书 API 写入文档。密钥不进网页，安全。

---

## 一、准备飞书自建应用（1 次）

1. 打开 https://open.feishu.cn/ ，用你的飞书账号登录 →【开发者后台】→【创建企业自建应用】。
2. 记下 **App ID** 和 **App Secret**（在「凭证与基础信息」里）。
3. 左侧【权限管理】→ 开通 **`docx:document:write`（文档:文档编辑）** 等文档写权限 → 保存。
4. 左侧【版本管理与发布】→ 创建版本 → 申请发布（企业内可见即可）。
5. 在飞书里**新建 4 个「文档」（新版 docx）**，分别用作 理财 / 养生 / AI / 运营。打开每个文档，URL 里 `…/docx/<token>` 那段 `<token>` 就是文档 token，记下来。

> 这 4 个文档建议**只用来存对应分类的笔记**，因为推送时会先清空再写入，避免和别的的内容混在一起。

## 二、创建阿里云函数（1 次）

1. 注册/登录 https://www.aliyun.com/ ，完成**实名认证**（函数计算按量计费，有免费额度，日常几乎零成本）。
2. 控制台搜索「**函数计算 FC**」→ 开通服务 → 选区域（建议「华东1 杭州」，国内访问快）。
3. 左侧【服务列表】→ 创建服务（名字随便，如 `qiaoqiao-feishu`）。
4. 进入服务 →【创建函数】：
   - 创建方式：**从零开始创建**
   - 运行环境：**Node.js 18 / 20**
   - 函数名称：`pushFeishu`
   - 请求模式（HTTP 触发器）：**默认（Proxy 代理模式）**
   - 触发器：**HTTP 触发器**；认证方式：**无需认证**（否则前端浏览器调不了）
   - 其他默认 → 创建。
5. 在函数代码里，**把默认代码全删掉，粘贴 `feishu-fc/index.js` 的内容**（本项目根目录里有，也可从本指南同源文件复制）。点【部署】。
6. 左侧【配置】→【环境变量】→ 新增以下变量（值填你自己的）：

   | 变量名 | 值 |
   |---|---|
   | `FEISHU_APP_ID` | 飞书应用 App ID |
   | `FEISHU_APP_SECRET` | 飞书应用 App Secret |
   | `FEISHU_DOC_LICAI` | 理财文档 token |
   | `FEISHU_DOC_YANGSHENG` | 养生文档 token |
   | `FEISHU_DOC_AI` | AI 文档 token |
   | `FEISHU_DOC_YUNYING` | 运营文档 token |
   | `FEISHU_PUSH_TOKEN` | （可选）任取一串字符，如 `abc123`，网页也要填一样的 |

7. 【配置】→【运行时】把**超时时间调到 30 秒**（飞书多次 API 调用可能超过默认 3 秒）。
8. 在函数【概览】里复制 **公网访问地址（HTTP 触发器 URL）**，形如：
   `https://<account-id>.cn-hangzhou.fc.aliyuncs.com/2016-08-15/proxy/qiaoqiao-feishu/pushFeishu/`

## 三、在桥桥工作台填地址

1. 打开桥桥工作台（手机 Safari 添加到主屏幕那个，或 `https://qiao-zhen.github.io`）。
2. 进「知识库笔记」→ 点 **⚙ 飞书设置**。
3. 填入：
   - **函数地址**：上面复制的公网 URL
   - **推送密钥**：和 `FEISHU_PUSH_TOKEN` 一致（没设就留空）
4. 点【保存设置】。

## 四、使用

在知识库笔记页，点 **导出到飞书** → 直接把 4 类笔记推到对应飞书文档。
（没填函数地址时，按钮会退化为「下载 JSON」，你仍可本机跑 `node push_feishu.js` 推送。）

---

## 排错

- **跨域/网络错误**：确认函数 HTTP 触发器认证方式是「无需认证」，且函数里已正确返回 `Access-Control-Allow-Origin` 头（代码已带）。
- **401/密钥失败**：检查 6 个环境变量是否填对、`FEISHU_APP_SECRET` 没多空格、应用已发布且权限已开通。
- **某类显示 skip(未配置文档token)**：说明该分类对应的 `FEISHU_DOC_xxx` 环境变量没填。
- **推送成功但飞书文档没内容**：确认文档是「新版文档(docx)」而非旧版，且 token 取的是 URL 里 `docx/` 之后的那段。
